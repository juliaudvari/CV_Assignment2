# Computer Vision Assignment 2 — use Python 3.10–3.13 (TensorFlow has no wheel for 3.14 yet).
#
# Windows + OneDrive: a venv inside this folder often breaks on TensorFlow (path too long).
# RECOMMENDED (no admin): short-path venv — already used successfully for this project:
#   py -3.13 -m venv C:\venv\cv_ca2
#   C:\venv\cv_ca2\Scripts\activate
#   python -m pip install -r "<full path to this requirements.txt>"
# Or enable long paths: https://pip.pypa.io/warnings/enable-long-paths

tensorflow>=2.15.0
tensorboard>=2.15.0
matplotlib>=3.7.0
numpy>=1.26.0
scikit-learn>=1.3.0
seaborn>=0.13.0
keras-tuner>=1.4.0
opencv-python-headless>=4.8.0
python-docx>=1.0.0
# Notebook (brief prefers Jupyter); install on Windows too — use a short path venv if pip hits path-length limits.
jupyter>=1.0.0
ipykernel>=6.0.0
